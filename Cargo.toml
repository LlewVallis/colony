[package]
name = "colony"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = "0.5.1"
rand = { version = "0.8.5", features = ["small_rng"] }

[[bench]]
name = "benches"
harness = false
